class RecentFile {
  String? holdername, category, branch, city, floor, barcode, room, date;
  RecentFile({
    required this.barcode,
    required this.branch,
    required this.category,
    required this.city,
    required this.date,
    required this.floor,
    required this.holdername,
  });
}
